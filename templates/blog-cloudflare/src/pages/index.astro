---
import {
	getEmDashCollection,
	getTermsForEntries,
	getSiteSettings,
} from "emdash";
import { Image } from "emdash/ui";
import Base from "../layouts/Base.astro";
import ReaderAuthBar from "../components/ReaderAuthBar.astro";
import { resolveBlogSiteIdentity } from "../utils/site-identity";
import creatinaPoteImg from "../assets/creatina-pote.webp";
import creatinaLaudoImg from "../assets/creatina-laudo.webp";

// Fetch generoso: 30 posts em DESC. Vamos splittar em "momento" (5 mais
// recentes), "mais lidos" (8 mais antigos) e "recentes" (6 do meio sem
// repetir). Em produção (~21 posts) cabe tudo sem N+1.
const POSTS_TO_FETCH = 30;

const [{ entries: allPosts, cacheHint }, settings] = await Promise.all([
	getEmDashCollection("posts", {
		orderBy: { published_at: "desc" },
		limit: POSTS_TO_FETCH,
	}),
	getSiteSettings(),
]);
const { siteTitle, siteTagline } = resolveBlogSiteIdentity(settings);

if (Astro.cache?.enabled) Astro.cache.set(cacheHint);

// Quando o leitor esta logado via FB, o HTML inclui nome/avatar dele
// (ReaderAuthBar). Pra nao vazar identidade pelo cache compartilhado,
// marcamos uncacheable nesse caso. Mesmo padrao da [slug].astro.
const hasReaderCookie = (Astro.request.headers.get("cookie") || "").includes("reader_session=");
Astro.response.headers.set(
	"Cache-Control",
	hasReaderCookie
		? "private, no-store"
		: "public, s-maxage=60, stale-while-revalidate=86400",
);

// Section 1 — Assuntos do momento: 2 large + 3 small (5 mais recentes)
const trending = allPosts.slice(0, 5);
const trendingLarge = trending.slice(0, 2);
const trendingSmall = trending.slice(2, 5);
const usedIds = new Set(trending.map((p) => p.data.id));

// Section 3 — Mais lidos: 8 mais antigos (proxy de "tempo exposto")
const mostRead: typeof allPosts = [];
for (let i = allPosts.length - 1; i >= 0 && mostRead.length < 8; i--) {
	const post = allPosts[i];
	if (usedIds.has(post.data.id)) continue;
	mostRead.push(post);
	usedIds.add(post.data.id);
}

// Section 5 — Conteúdos recentes (só títulos): 6 do meio sem repetir
const recentTitles: typeof allPosts = [];
for (const post of allPosts) {
	if (recentTitles.length >= 6) break;
	if (usedIds.has(post.data.id)) continue;
	recentTitles.push(post);
	usedIds.add(post.data.id);
}

// Batch query: categorias dos posts que aparecem como card (não dos só-título)
const cardIds = [
	...trending.map((p) => p.data.id),
	...mostRead.map((p) => p.data.id),
];
const categoriesByEntry = await getTermsForEntries(
	"posts",
	cardIds,
	"category",
);

type CategoryRef = { slug: string; label: string };

function withCategory(post: typeof allPosts[0]) {
	return {
		post,
		category: ((categoriesByEntry.get(post.data.id) ?? [])[0]) as
			| CategoryRef
			| undefined,
	};
}

const trendingLargeWithCat = trendingLarge.map(withCategory);
const trendingSmallWithCat = trendingSmall.map(withCategory);
const mostReadWithCat = mostRead.map(withCategory);


const slugFor = (post: typeof allPosts[0]) => post.slug ?? post.id;
---

<Base
	title={siteTitle}
	description={siteTagline}
	canonical={`${Astro.url.origin}/blog`}
>
	<h1 class="home-page-heading">{siteTitle}</h1>

	{
		allPosts.length === 0 ? (
			<section class="empty-state">
				<h2>Nenhum artigo ainda</h2>
				<p>Crie seu primeiro artigo no painel administrativo.</p>
				<a href="/_emdash/admin/content/posts/new" class="btn">
					Criar um artigo
				</a>
			</section>
		) : (
			<div class="home-content">
				{/* ====== 0. INTRO — quem somos (SEO + boas-vindas) ====== */}
				<section class="home-intro" aria-label="Sobre o Blog DouraVita">
					<p class="home-intro-text">
						Bem-vindo ao <strong>Blog DouraVita</strong> — conteúdo de saúde
						e bem-estar baseado em ciência para adultos 55+. Aqui no nosso
						blog você encontra artigos sobre suplementação, movimento,
						memória, longevidade e qualidade de vida.
					</p>
				</section>

				{/* ====== 0.5. CONECTAR FACEBOOK — sessao global do leitor ====== */}
				<section class="home-auth" aria-label="Conectar com Facebook">
					<ReaderAuthBar
						class="home-auth__bar"
						pitch="Conecte seu Facebook e fique logado em todo o blog — comente nos artigos sem precisar entrar de novo."
						loggedLabel="Conectado como"
					/>
				</section>

				{/* ====== 1. ASSUNTOS DO MOMENTO ====== */}
				<section class="section trending">
					<header class="section-header">
						<h2 class="section-title">Assuntos do momento</h2>
					</header>

					<div class="trending-large-grid">
						{trendingLargeWithCat.map(({ post, category }) => (
							<article class="card-large">
								{post.data.featured_image && (
									<a
										href={`/blog/posts/${slugFor(post)}`}
										class="card-large-image-link"
										aria-label={post.data.title}
									>
										<div class="card-large-image">
											<Image image={post.data.featured_image} />
										</div>
									</a>
								)}
								<div class="card-large-body">
									{category && (
										<a
											href={`/blog/category/${category.slug}`}
											class="badge"
										>
											{category.label}
										</a>
									)}
									<a
										href={`/blog/posts/${slugFor(post)}`}
										class="card-title-link"
									>
										<h3 class="card-large-title">
											{post.data.title ?? "Sem título"}
										</h3>
									</a>
									{(post.data.excerpt ?? post.data.description) && (
										<p class="card-large-excerpt">
											{post.data.excerpt ?? post.data.description}
										</p>
									)}
								</div>
							</article>
						))}
					</div>

					<div class="trending-small-list">
						{trendingSmallWithCat.map(({ post, category }) => (
							<article class="card-small">
								{post.data.featured_image && (
									<a
										href={`/blog/posts/${slugFor(post)}`}
										class="card-small-image-link"
										aria-label={post.data.title}
									>
										<Image image={post.data.featured_image} />
									</a>
								)}
								<div class="card-small-body">
									{category && (
										<a
											href={`/blog/category/${category.slug}`}
											class="badge"
										>
											{category.label}
										</a>
									)}
									<a
										href={`/blog/posts/${slugFor(post)}`}
										class="card-title-link"
									>
										<h3 class="card-small-title">
											{post.data.title ?? "Sem título"}
										</h3>
									</a>
									{(post.data.excerpt ?? post.data.description) && (
										<p class="card-small-excerpt">
											{post.data.excerpt ?? post.data.description}
										</p>
									)}
								</div>
							</article>
						))}
					</div>
				</section>


				{/* ====== 2.5 SARCOPENIA + CREATINA (educacional) ====== */}
				<section class="section edu-section">
					<a href="/blog/category/suplementacao" class="edu-card-link">
						<article class="edu-card">
							<header class="edu-header">
								<span class="edu-eyebrow">Aprofunde-se</span>
								<h2 class="edu-title">
									Sarcopenia: o que a ciência diz (e como a creatina ajuda)
								</h2>
							</header>

							<div class="edu-body">
								<p>
									A <strong>sarcopenia</strong>, perda progressiva de massa
									e força muscular ligada ao envelhecimento, atinge cerca
									de{" "}
									<strong>1 em cada 3 brasileiros acima de 60 anos</strong>,
									segundo o estudo SABE (Saúde, Bem-estar e Envelhecimento)
									da USP. Ela está diretamente ligada a quedas, perda de
									independência e mortalidade precoce. Mas pode ser
									desacelerada (e em parte revertida) com duas intervenções
									simples: <strong>musculação regular</strong> e{" "}
									<strong>ingestão adequada de proteína</strong>.
								</p>

								<h3 class="edu-subtitle">O que a ciência mostra</h3>
								<p>
									Uma{" "}
									<strong>meta-análise de Chilibeck et al. (2017)</strong>{" "}
									reuniu 22 estudos com idosos e demonstrou que combinar
									creatina (3 a 5 g/dia) com treino de força 2 a 3x por
									semana resulta, em média, em <strong>1,4 kg a mais de
									massa muscular magra</strong> em comparação à musculação
									isolada. A revisão de{" "}
									<strong>Forbes et al. (2021)</strong> confirma o perfil de
									segurança da creatina em adultos 55+ e aponta ganhos
									adicionais em densidade óssea e desempenho cognitivo.
								</p>

								<h3 class="edu-subtitle">A creatina Douravita</h3>
								<div class="edu-product">
									<img
										src={creatinaPoteImg.src}
										alt="Pote da creatina monohidratada DouraVita 100% pura"
										class="edu-product-img"
										loading="lazy"
										width="320"
										height="320"
									/>
									<div class="edu-product-body">
										<p>
											A creatina monohidratada Douravita foi formulada
											pensando no público 55+: <strong>5 g por dia</strong>,
											sabor neutro, sem corantes nem aditivos
											desnecessários. Dissolve fácil em água, suco ou
											iogurte, e funciona melhor quando combinada com uma
											refeição rica em proteína e treino de força regular.
										</p>
										<a
											href={creatinaLaudoImg.src}
											target="_blank"
											rel="noopener"
											class="edu-laudo-link"
											onclick="event.stopPropagation();"
										>
											<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
												<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
												<polyline points="14 2 14 8 20 8"></polyline>
												<line x1="16" y1="13" x2="8" y2="13"></line>
												<line x1="16" y1="17" x2="8" y2="17"></line>
											</svg>
											<span>
												Veja o laudo analítico Eurofins
												<small>99,10% creatina monohidratada</small>
											</span>
										</a>
									</div>
								</div>
							</div>
						</article>
					</a>
				</section>

								{/* ====== 2.7 FERRAMENTAS (categoria, conteudo interativo) ====== */}
				<section class="section tools-section">
					<div class="tools-card">
						<header class="tools-header">
							<span class="tools-eyebrow">Ferramentas</span>
							<h2 class="tools-title">Teste-se, calcule, avalie</h2>
							<p class="tools-subtitle">
								Quiz e calculadoras gratuitas pensadas pra quem tem 55+.
								Escolha a que faz sentido pra você.
							</p>
						</header>
					<div class="tools-grid">
						<a href="/blog/ferramentas/sarcopenia" class="tools-item">
							<span class="tools-item-icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"></rect><path d="M9 9h6"></path><path d="M9 13h6"></path><path d="M9 17h3"></path></svg></span>
							<span class="tools-item-body">
								<span class="tools-item-eyebrow">Quiz</span>
								<span class="tools-item-title">Você está em risco de sarcopenia?</span>
								<span class="tools-item-meta">6 perguntas · 1 min</span>
							</span>
							<svg class="tools-item-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="12" y1="19" x2="12" y2="5"></line><polyline points="5 12 12 5 19 12"></polyline></svg>
						</a>
						<a href="/blog/ferramentas/imc-55" class="tools-item">
							<span class="tools-item-icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="2" width="16" height="20" rx="2"></rect><line x1="8" y1="6" x2="16" y2="6"></line><line x1="8" y1="10" x2="10" y2="10"></line><line x1="14" y1="10" x2="16" y2="10"></line><line x1="8" y1="14" x2="10" y2="14"></line><line x1="14" y1="14" x2="16" y2="14"></line><line x1="8" y1="18" x2="10" y2="18"></line><line x1="14" y1="18" x2="16" y2="18"></line></svg></span>
							<span class="tools-item-body">
								<span class="tools-item-eyebrow">Calculadora</span>
								<span class="tools-item-title">IMC ajustado para 55+</span>
								<span class="tools-item-meta">Idade, peso, altura · 10 segundos</span>
							</span>
							<svg class="tools-item-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="12" y1="19" x2="12" y2="5"></line><polyline points="5 12 12 5 19 12"></polyline></svg>
						</a>
						<a href="/blog/ferramentas/proteina-diaria" class="tools-item">
							<span class="tools-item-icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="2" width="16" height="20" rx="2"></rect><line x1="8" y1="6" x2="16" y2="6"></line><line x1="8" y1="10" x2="10" y2="10"></line><line x1="14" y1="10" x2="16" y2="10"></line><line x1="8" y1="14" x2="10" y2="14"></line><line x1="14" y1="14" x2="16" y2="14"></line><line x1="8" y1="18" x2="10" y2="18"></line><line x1="14" y1="18" x2="16" y2="18"></line></svg></span>
							<span class="tools-item-body">
								<span class="tools-item-eyebrow">Calculadora</span>
								<span class="tools-item-title">Proteína diária para 55+</span>
								<span class="tools-item-meta">Peso + nível de atividade · 10 segundos</span>
							</span>
							<svg class="tools-item-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="12" y1="19" x2="12" y2="5"></line><polyline points="5 12 12 5 19 12"></polyline></svg>
						</a>
						<a href="/blog/ferramentas/idade-biologica" class="tools-item">
							<span class="tools-item-icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="2" width="16" height="20" rx="2"></rect><line x1="8" y1="6" x2="16" y2="6"></line><line x1="8" y1="10" x2="10" y2="10"></line><line x1="14" y1="10" x2="16" y2="10"></line><line x1="8" y1="14" x2="10" y2="14"></line><line x1="14" y1="14" x2="16" y2="14"></line><line x1="8" y1="18" x2="10" y2="18"></line><line x1="14" y1="18" x2="16" y2="18"></line></svg></span>
							<span class="tools-item-body">
								<span class="tools-item-eyebrow">Calculadora</span>
								<span class="tools-item-title">Idade biológica vs cronológica</span>
								<span class="tools-item-meta">10 hábitos · 2 min</span>
							</span>
							<svg class="tools-item-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="12" y1="19" x2="12" y2="5"></line><polyline points="5 12 12 5 19 12"></polyline></svg>
						</a>
					</div>
					</div>
				</section>

				{/* ====== 3. MAIS LIDOS ====== */}
				{mostReadWithCat.length > 0 && (
					<section class="section most-read">
						<header class="section-header">
							<h2 class="section-title">Mais lidos</h2>
						</header>
						<div class="most-read-grid">
							{mostReadWithCat.map(({ post, category }) => (
								<article class="card-tile">
									{post.data.featured_image && (
										<a
											href={`/blog/posts/${slugFor(post)}`}
											class="card-tile-image-link"
											aria-label={post.data.title}
										>
											<div class="card-tile-image">
												<Image image={post.data.featured_image} />
											</div>
										</a>
									)}
									<div class="card-tile-body">
										{category && (
											<a
												href={`/blog/category/${category.slug}`}
												class="badge"
											>
												{category.label}
											</a>
										)}
										<a
											href={`/blog/posts/${slugFor(post)}`}
											class="card-title-link"
										>
											<h3 class="card-tile-title">
												{post.data.title ?? "Sem título"}
											</h3>
										</a>
									</div>
								</article>
							))}
						</div>
					</section>
				)}

				{/* ====== 4.5 FRASE INSPIRADORA ====== */}
				<section class="inspirational-quote" aria-label="Mensagem">
					<div class="inspirational-quote-inner">
						<blockquote class="inspirational-quote-text">
							Cuidar de si mesmo é o melhor presente que você pode dar a quem te ama.
						</blockquote>
					</div>
				</section>

				{/* ====== 5. CONTEÚDOS RECENTES (só títulos) ====== */}
				{recentTitles.length > 0 && (
					<section class="section recent">
						<header class="section-header">
							<h2 class="section-title">Conteúdos mais recentes</h2>
						</header>
						<ul class="recent-list">
							{recentTitles.map((post) => (
								<li class="recent-item">
									<a
										href={`/blog/posts/${slugFor(post)}`}
										class="recent-link"
									>
										<h3 class="recent-title">
											{post.data.title ?? "Sem título"}
										</h3>
									</a>
									{post.data.publishedAt && (
										<time class="recent-date">
											Criado em{" "}
											{post.data.publishedAt.toLocaleDateString("pt-BR")}
										</time>
									)}
								</li>
							))}
						</ul>
					</section>
				)}

				{/* ====== 6. VER MAIS CONTEÚDOS ====== */}
				<section class="see-all-section">
					<a href="/blog/posts" class="see-all-btn">
						VER MAIS CONTEÚDOS <span aria-hidden="true">→</span>
					</a>
				</section>
			</div>
		)
	}

</Base>

<style>
	/* ===== Globals ===== */
	.home-page-heading {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	.home-content {
		max-width: var(--wide-width);
		margin: 0 auto;
		padding: var(--spacing-8) var(--spacing-4) var(--spacing-16);
	}

	/* Intro de boas-vindas no topo da home — reforça que isto é o BLOG
	 * (sinal de SEO pra busca "blog douravita"). Visualmente discreta,
	 * separa-se do hero "Assuntos do momento" via borda inferior fina. */
	.home-intro {
		margin-bottom: var(--spacing-10);
		padding-bottom: var(--spacing-6);
		border-bottom: 1px solid var(--color-border-subtle);
	}

	.home-intro-text {
		font-size: var(--font-size-base);
		line-height: var(--leading-relaxed);
		color: var(--color-text-secondary);
		max-width: 760px;
		margin-left: auto;
		margin-right: auto;
		text-align: center;
	}

	.home-intro-text strong {
		color: var(--color-text);
		font-weight: 700;
	}

	@media (min-width: 700px) {
		.home-intro-text {
			font-size: var(--font-size-lg);
		}
	}

	.home-auth {
		margin: calc(var(--spacing-10) * -1 + var(--spacing-2)) auto var(--spacing-10);
		max-width: 760px;
	}

	.home-auth :global(.home-auth__bar) {
		background: color-mix(in srgb, var(--color-accent) 4%, var(--color-bg));
		border-color: color-mix(in srgb, var(--color-accent) 18%, var(--color-border));
	}

	.section {
		margin-bottom: var(--spacing-16);
	}

	.section-header {
		margin-bottom: var(--spacing-6);
	}

	.section-title {
		font-size: var(--font-size-2xl);
		font-weight: 700;
		letter-spacing: var(--tracking-tight);
		line-height: var(--leading-snug);
	}

	.badge {
		display: inline-block;
		align-self: start;
		padding: var(--tag-padding-y) var(--spacing-3);
		font-size: var(--font-size-xs);
		font-weight: 500;
		text-transform: uppercase;
		letter-spacing: var(--tracking-wide);
		color: var(--color-accent);
		background: var(--color-accent-ring, var(--color-surface));
		border-radius: 999px;
		text-decoration: none;
		transition: background var(--transition-fast);
	}

	.badge:hover {
		background: var(--color-border);
	}

	.card-title-link {
		text-decoration: none;
		color: inherit;
	}

	/* ===== 1. Assuntos do momento ===== */

	/* Mobile-first: 2 large empilhados + 3 small empilhados */
	.trending-large-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: var(--spacing-8);
		margin-bottom: var(--spacing-8);
	}

	.card-large {
		display: flex;
		flex-direction: column;
	}

	.card-large-image-link {
		display: block;
		text-decoration: none;
		color: inherit;
	}

	.card-large-image {
		aspect-ratio: 16 / 9;
		max-height: 320px;
		overflow: hidden;
		border-radius: var(--radius-lg);
		background: var(--color-surface);
		margin-bottom: var(--spacing-3);
	}

	.card-large-image img {
		width: 100% !important;
		height: 100% !important;
		object-fit: cover !important;
		transition: transform 0.3s ease;
	}

	.card-large-image-link:hover .card-large-image img {
		transform: scale(1.03);
	}

	.card-large-body {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-2);
	}

	.card-large-title {
		font-size: var(--font-size-xl);
		font-weight: 700;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-snug);
		transition: color var(--transition-fast);
	}

	.card-title-link:hover .card-large-title,
	.card-title-link:hover .card-small-title,
	.card-title-link:hover .card-tile-title {
		color: var(--color-accent);
	}

	.card-large-excerpt {
		font-size: var(--font-size-base);
		line-height: var(--leading-relaxed);
		color: var(--color-text-secondary);
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	.trending-small-list {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-5);
	}

	.card-small {
		display: grid;
		grid-template-columns: 110px 1fr;
		gap: var(--spacing-3);
		align-items: start;
	}

	.card-small-image-link {
		display: block;
		overflow: hidden;
		border-radius: var(--radius);
		background: var(--color-surface);
	}

	.card-small-image-link img {
		width: 100% !important;
		height: 100% !important;
		aspect-ratio: 4 / 3;
		object-fit: cover !important;
		transition: transform 0.3s ease;
	}

	.card-small-image-link:hover img {
		transform: scale(1.04);
	}

	.card-small-body {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-2);
		min-width: 0;
	}

	.card-small-title {
		font-size: var(--font-size-base);
		font-weight: 600;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-snug);
		transition: color var(--transition-fast);
	}

	.card-small-excerpt {
		font-size: var(--font-size-sm);
		line-height: var(--leading-snug);
		color: var(--color-text-secondary);
		margin: 0;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	/* ===== 3. Mais lidos ===== */
	.most-read-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--spacing-5) var(--spacing-4);
	}

	.card-tile {
		display: flex;
		flex-direction: column;
	}

	.card-tile-image-link {
		display: block;
		text-decoration: none;
		color: inherit;
	}

	.card-tile-image {
		aspect-ratio: 4 / 3;
		overflow: hidden;
		border-radius: var(--radius);
		background: var(--color-surface);
		margin-bottom: var(--spacing-3);
	}

	.card-tile-image img {
		width: 100% !important;
		height: 100% !important;
		object-fit: cover !important;
		transition: transform 0.3s ease;
	}

	.card-tile-image-link:hover .card-tile-image img {
		transform: scale(1.03);
	}

	.card-tile-body {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-2);
	}

	.card-tile-title {
		font-size: var(--font-size-base);
		font-weight: 600;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-snug);
		transition: color var(--transition-fast);
	}

	/* ===== 5. Conteúdos recentes ===== */
	.recent-list {
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.recent-item {
		padding: var(--spacing-5) 0;
		border-bottom: 1px solid var(--color-border-subtle);
	}

	.recent-item:first-child {
		padding-top: 0;
	}

	.recent-item:last-child {
		border-bottom: none;
	}

	.recent-link {
		display: block;
		text-decoration: none;
		color: inherit;
	}

	.recent-title {
		font-size: var(--font-size-lg);
		font-weight: 600;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-snug);
		margin-bottom: var(--spacing-1);
		transition: color var(--transition-fast);
	}

	.recent-link:hover .recent-title {
		color: var(--color-accent);
	}

	.recent-date {
		display: block;
		font-size: var(--font-size-sm);
		color: var(--color-muted);
	}

	/* ===== 6. Ver mais conteúdos ===== */
	.see-all-section {
		display: flex;
		justify-content: center;
		margin-top: var(--spacing-8);
	}

	.see-all-btn {
		display: inline-flex;
		align-items: center;
		gap: var(--spacing-2);
		padding: var(--spacing-3) var(--spacing-6);
		color: var(--color-accent);
		border: 1px solid var(--color-accent);
		border-radius: 999px;
		font-size: var(--font-size-sm);
		font-weight: 600;
		letter-spacing: var(--tracking-wide);
		text-decoration: none;
		transition: all var(--transition-fast);
	}

	.see-all-btn:hover {
		background: var(--color-accent);
		color: var(--color-on-accent);
	}

	/* ===== Empty state ===== */
	.empty-state {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: var(--spacing-3);
		text-align: center;
		padding: var(--spacing-20) var(--spacing-6);
		max-width: 400px;
		margin: 0 auto;
	}

	.empty-state h2 {
		font-size: var(--font-size-2xl);
		font-weight: 600;
	}

	.empty-state p {
		color: var(--color-muted);
	}

	.btn {
		display: inline-block;
		margin-top: var(--spacing-4);
		padding: var(--spacing-3) var(--spacing-6);
		background: var(--color-accent);
		color: var(--color-on-accent);
		text-decoration: none;
		border-radius: var(--radius);
		font-size: var(--font-size-sm);
		font-weight: 500;
		transition: background var(--transition-fast);
	}

	/* ===== Educational section: sarcopenia + creatina ===== */
	.edu-card {
		background: var(--color-bg-subtle, var(--color-surface));
		border-radius: var(--radius-lg);
		padding: var(--spacing-6) var(--spacing-5);
		border-left: 4px solid var(--color-accent);
	}

	.edu-header {
		margin-bottom: var(--spacing-5);
	}

	.edu-eyebrow {
		display: inline-block;
		font-size: var(--font-size-xs);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: var(--tracking-wider);
		color: var(--color-accent);
		margin-bottom: var(--spacing-2);
	}

	.edu-title {
		font-size: clamp(1.5rem, 4vw, 2rem);
		font-weight: 700;
		line-height: var(--leading-tight);
		letter-spacing: var(--tracking-tight);
		color: var(--color-text);
	}

	.edu-body {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-4);
	}

	.edu-body p {
		font-size: var(--font-size-base);
		line-height: var(--leading-relaxed);
		color: var(--color-text);
	}

	.edu-body strong {
		font-weight: 700;
		color: var(--color-text);
	}

	.edu-subtitle {
		font-size: var(--font-size-lg);
		font-weight: 700;
		line-height: var(--leading-snug);
		color: var(--color-accent);
		margin-top: var(--spacing-2);
	}

	/* Card todo clicavel — sem CTA pilula explicito */
	.edu-card-link {
		display: block;
		text-decoration: none;
		color: inherit;
		transition: transform var(--transition-fast), box-shadow var(--transition-fast);
	}

	.edu-card-link:hover .edu-card,
	.edu-card-link:focus-visible .edu-card {
		transform: translateY(-2px);
		box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
	}

	.edu-card-link:hover .edu-title,
	.edu-card-link:focus-visible .edu-title {
		color: var(--color-accent);
	}

	.edu-card-link:focus-visible {
		outline: 2px solid var(--color-accent);
		outline-offset: 4px;
		border-radius: var(--radius-lg);
	}

	.edu-card {
		transition: transform var(--transition-fast), box-shadow var(--transition-fast);
	}

	.edu-title {
		transition: color var(--transition-fast);
	}

	/* ===== Product (pote + laudo) dentro do edu-card ===== */
	.edu-product {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-4);
		align-items: stretch;
	}

	.edu-product-img {
		width: 100%;
		max-width: 280px;
		height: auto;
		aspect-ratio: 1 / 1;
		object-fit: cover;
		border-radius: var(--radius-lg);
		align-self: center;
		background: #0a0a1a;
	}

	.edu-product-body {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-3);
		flex: 1;
		min-width: 0;
	}

	.edu-laudo-link {
		display: inline-flex;
		align-items: center;
		gap: var(--spacing-3);
		align-self: start;
		padding: var(--spacing-3) var(--spacing-4);
		background: var(--color-bg);
		color: var(--color-text);
		border: 1px solid var(--color-border);
		border-radius: var(--radius);
		text-decoration: none;
		font-size: var(--font-size-sm);
		font-weight: 600;
		transition: border-color var(--transition-fast), background var(--transition-fast);
	}

	.edu-laudo-link:hover,
	.edu-laudo-link:focus-visible {
		border-color: var(--color-accent);
		background: var(--color-accent-ring, var(--color-surface));
		outline: none;
	}

	.edu-laudo-link svg {
		width: 22px;
		height: 22px;
		flex-shrink: 0;
		color: var(--color-accent);
	}

	.edu-laudo-link span {
		display: flex;
		flex-direction: column;
		gap: 2px;
		line-height: var(--leading-snug);
	}

	.edu-laudo-link small {
		font-size: var(--font-size-xs);
		font-weight: 400;
		color: var(--color-muted);
	}

	/* Desktop: pote a esquerda, texto a direita */
	@media (min-width: 600px) {
		.edu-product {
			flex-direction: row;
			gap: var(--spacing-6);
		}

		.edu-product-img {
			width: 220px;
			max-width: 220px;
			flex-shrink: 0;
			align-self: flex-start;
		}
	}

		/* ===== Ferramentas (categoria de conteudo interativo) ===== */
	.tools-card {
		background: var(--color-bg-subtle, var(--color-surface));
		border-radius: var(--radius-lg);
		padding: var(--spacing-6) var(--spacing-5);
		border-left: 4px solid var(--color-accent);
	}

	.tools-header {
		margin-bottom: var(--spacing-5);
	}

	.tools-eyebrow {
		display: inline-block;
		font-size: var(--font-size-xs);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: var(--tracking-wider);
		color: var(--color-accent);
		margin-bottom: var(--spacing-2);
	}

	.tools-title {
		font-size: clamp(1.5rem, 4vw, 2rem);
		font-weight: 700;
		line-height: var(--leading-tight);
		letter-spacing: var(--tracking-tight);
		color: var(--color-text);
	}

	.tools-subtitle {
		font-size: var(--font-size-base);
		line-height: var(--leading-relaxed);
		color: var(--color-text);
		margin-top: var(--spacing-3);
	}

	.tools-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: var(--spacing-3);
	}

	@media (min-width: 700px) {
		.tools-grid {
			grid-template-columns: 1fr 1fr;
		}
	}

	/* Card interno clicavel da ferramenta */
	.tools-item {
		display: flex;
		align-items: center;
		gap: var(--spacing-4);
		padding: var(--spacing-4) var(--spacing-5);
		background: var(--color-surface);
		border: 1px solid var(--color-border, rgba(0, 0, 0, 0.08));
		border-radius: var(--radius-md);
		text-decoration: none;
		color: inherit;
		transition: transform var(--transition-fast), box-shadow var(--transition-fast), border-color var(--transition-fast);
	}

	.tools-item:hover,
	.tools-item:focus-visible {
		transform: translateY(-2px);
		box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
		border-color: var(--color-accent);
		outline: none;
	}

	.tools-item-icon {
		flex-shrink: 0;
		width: 44px;
		height: 44px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: var(--radius-md);
		background: rgba(0, 0, 0, 0.04);
		color: var(--color-accent);
	}

	.tools-item-icon svg {
		width: 22px;
		height: 22px;
	}

	.tools-item-body {
		display: flex;
		flex-direction: column;
		gap: 2px;
		flex: 1;
		min-width: 0;
	}

	.tools-item-eyebrow {
		font-size: var(--font-size-xs);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: var(--tracking-wider);
		color: var(--color-accent);
	}

	.tools-item-title {
		font-size: var(--font-size-base);
		font-weight: 700;
		line-height: var(--leading-snug);
		color: var(--color-text);
	}

	.tools-item-meta {
		font-size: var(--font-size-sm);
		color: var(--color-text-muted, var(--color-text));
		opacity: 0.75;
	}

	.tools-item-arrow {
		flex-shrink: 0;
		width: 18px;
		height: 18px;
		color: var(--color-text-muted, var(--color-text));
		opacity: 0.6;
		transition: transform var(--transition-fast), opacity var(--transition-fast), color var(--transition-fast);
	}

	.tools-item:hover .tools-item-arrow,
	.tools-item:focus-visible .tools-item-arrow {
		transform: translateY(-3px);
		opacity: 1;
		color: var(--color-accent);
	}

	.tools-soon {
		margin-top: var(--spacing-4);
		font-size: var(--font-size-sm);
		font-style: italic;
		color: var(--color-text-muted, var(--color-text));
		opacity: 0.75;
		text-align: center;
	}

	/* ===== Frase inspiradora (antes de 'Conteudos mais recentes') ===== */
	.inspirational-quote {
		background: var(--color-bg-subtle);
		padding: var(--spacing-12) var(--spacing-6);
		border-radius: var(--radius-lg);
		margin-bottom: var(--spacing-16);
	}

	.inspirational-quote-inner {
		max-width: 720px;
		margin: 0 auto;
		text-align: center;
	}

	.inspirational-quote-text {
		font-family: Georgia, "Times New Roman", "DejaVu Serif", serif;
		font-size: clamp(var(--font-size-xl), 3vw, var(--font-size-3xl));
		font-weight: 400;
		font-style: italic;
		line-height: 1.4;
		color: var(--color-text);
		letter-spacing: -0.01em;
		margin: 0;
		quotes: "“" "”";
	}

	.inspirational-quote-text::before {
		content: open-quote;
		display: block;
		font-size: 3em;
		line-height: 0.5;
		color: var(--color-accent);
		opacity: 0.4;
		margin-bottom: var(--spacing-4);
		font-style: normal;
	}

	.inspirational-quote-text::after {
		content: close-quote;
		display: none;
	}

	/* ===== Responsive ===== */

	/* Tablet: 2 colunas pros large cards + small ainda em coluna */
	@media (min-width: 700px) {
		.edu-card {
			padding: var(--spacing-8) var(--spacing-10);
		}

		.home-content {
			padding: var(--spacing-12) var(--spacing-6) var(--spacing-16);
		}

		.trending-large-grid {
			grid-template-columns: 1fr 1fr;
		}

		.card-large-title {
			font-size: var(--font-size-2xl);
		}

		.most-read-grid {
			grid-template-columns: repeat(4, 1fr);
			gap: var(--spacing-6) var(--spacing-5);
		}

		.card-tile-title {
			font-size: var(--font-size-lg);
		}

		.section-title {
			font-size: var(--font-size-3xl);
		}
	}

	/* Desktop: mais respiro entre seções */
	@media (min-width: 1000px) {
		.section {
			margin-bottom: var(--spacing-20);
		}

		.card-large-title {
			font-size: clamp(var(--font-size-xl), 2.4vw, var(--font-size-3xl));
		}

		.card-small {
			grid-template-columns: 140px 1fr;
			gap: var(--spacing-4);
		}

		.card-small-title {
			font-size: var(--font-size-lg);
		}
	}
</style>
